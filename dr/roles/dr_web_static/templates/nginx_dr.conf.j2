server {
    listen 8000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location = /dr-health {
        default_type text/plain;
        return 200 "DR WEB OK\n";
    }
}
